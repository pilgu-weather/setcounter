# Phone screenshots

The four `store-*.png` files are Play Console upload copies made from the
user-selected `KakaoTalk_20260812_122950420*.png` captures in the project root.
The source captures are preserved unchanged. The upload copies use a 1080 x
2160 opaque canvas so the complete UI remains visible while satisfying Google
Play's maximum 2:1 screenshot aspect ratio.
